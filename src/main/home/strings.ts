import { receive } from './receive/strings';
import { send } from './send/strings';

export const home = {
  receive,
  send,
  HomeScreen: {
    receiveLabel: '$(home.receive.label)',
    sendLabel: '$(home.send.label)',
  },
};
